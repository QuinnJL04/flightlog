// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  createdAt DateTime @default(now())

  // Clerk owns identity; this is our local mirror of it. Nullable because the
  // pre-Clerk seeded user has no Clerk account yet — it gets linked by email
  // the first time that person signs in. See src/lib/user.ts.
  clerkId String? @unique

  trips Trip[]
}

model Trip {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())

  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  flights Flight[]
}

model Airline {
  id       String  @id @default(cuid())
  name     String  @unique
  iataCode String? @unique

  flights Flight[]
}

model Flight {
  id            String   @id @default(cuid())
  origin        String
  destination   String
  departureDate DateTime
  createdAt     DateTime @default(now())

  tripId String
  trip   Trip   @relation(fields: [tripId], references: [id], onDelete: Cascade)

  airlineId String?
  airline   Airline? @relation(fields: [airlineId], references: [id])
}
